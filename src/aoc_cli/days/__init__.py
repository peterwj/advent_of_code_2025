"""Registry and utilities for Advent of Code day modules.

Each day module should expose a :func:`solve` function with signature:

    def solve(part: int, data: str) -> str:
        ...

The CLI will import ``aoc_cli.days.dayXX`` and call ``solve``.
"""
